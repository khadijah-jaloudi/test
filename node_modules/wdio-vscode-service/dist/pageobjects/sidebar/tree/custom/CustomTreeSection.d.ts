import { TreeSection } from '../TreeSection.js';
import { TreeItem } from '../../ViewItem.js';
import { AllViewSectionLocators } from '../../ViewSection.js';
import { IPageDecorator } from '../../../utils.js';
export interface CustomTreeSection extends IPageDecorator<AllViewSectionLocators> {
}
/**
 * Custom tree view, e.g. contributed by an extension
 *
 * @category Sidebar
 */
export declare class CustomTreeSection extends TreeSection {
    /**
     * @private
     */
    locatorKey: ("ViewSection" | "CustomTreeSection")[];
    getVisibleItems(): Promise<TreeItem[]>;
    findItem(label: string, maxLevel?: number): Promise<TreeItem | undefined>;
}
//# sourceMappingURL=CustomTreeSection.d.ts.map
import { ViewSection } from '../ViewSection.js';
import { TreeItem } from '../ViewItem.js';
/**
 * Abstract representation of a view section containing a tree
 */
export declare abstract class TreeSection extends ViewSection {
    openItem(...path: string[]): Promise<TreeItem[]>;
    abstract findItem(label: string, maxLevel?: number): Promise<TreeItem | undefined>;
    abstract getVisibleItems(): Promise<TreeItem[]>;
}
//# sourceMappingURL=TreeSection.d.ts.map